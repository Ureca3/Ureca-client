import React from 'react';

import { LoadingComponent } from '@/components/ui/fallback/LoadingComponent';

const loading = () => {
  return (
    <div>
      <LoadingComponent />
    </div>
  );
};

export default loading;
